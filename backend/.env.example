# ===========================================
# AI HTML Builder v2 - Environment Variables
# ===========================================
# Copy this file to .env and fill in your API keys
# cp .env.example .env

# --- API Keys (Required) ---
ANTHROPIC_API_KEY=sk-ant-...              # Claude Sonnet 4.5 - surgical edits
GOOGLE_API_KEY=AIza...                    # Gemini 2.5 Pro (creation) + Nano Banana Pro (images)

# --- Application ---
ENVIRONMENT=development                    # development | production
LOG_LEVEL=info                            # debug | info | warning | error
DEBUG=false

# --- Server ---
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=["http://localhost:5173"]    # Vite dev server default

# --- Database (SQLite - persisted via Docker volume) ---
DATABASE_PATH=./data/app.db

# --- Rate Limiting ---
RATE_LIMIT_REQUESTS=30                    # Per minute per session
RATE_LIMIT_WINDOW=60                      # Window in seconds

# --- Session ---
SESSION_TIMEOUT_HOURS=24

# --- File Upload ---
MAX_UPLOAD_SIZE_MB=50

# --- Models (Override defaults if needed) ---
# EDIT_MODEL=claude-sonnet-4-5-20250929
# CREATION_MODEL=gemini-2.5-pro
# IMAGE_MODEL=gemini-2.0-flash-preview-image-generation
